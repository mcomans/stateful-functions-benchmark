apiVersion: cloudstate.io/v1alpha1
kind: StatefulStore
metadata:
  name: inmemory-order-store
spec:
  type: InMemory

---
apiVersion: cloudstate.io/v1alpha1
kind: StatefulService
metadata:
  name: order-service
  labels:
    app: order-service
spec:
  datastore:
    name: inmemory-order-store
  containers:
    - image: benchmark-cloudstate-order-service
