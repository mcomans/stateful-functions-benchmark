apiVersion: cloudstate.io/v1alpha1
kind: StatefulStore
metadata:
  name: inmemory-product-store
spec:
  type: InMemory

---
apiVersion: cloudstate.io/v1alpha1
kind: StatefulService
metadata:
  name: product-service
  labels:
    app: product-service
spec:
  datastore:
    name: inmemory-product-store
  containers:
    - image: benchmark-cloudstate-product-service
