apiVersion: v1
kind: Service
metadata:
  name: benchmark-orleans-service
spec:
  selector:
    orleans/serviceId: benchmark-orleans
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 80
